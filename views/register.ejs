<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register</title>
    <script src="https://cdn.tailwindcss.com"></script> <!-- Tailwind CDN -->
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
</head>

<body class="flex flex-col items-center justify-center min-h-screen py-8 font-sans bg-gradient-to-r from-teal-400 via-blue-500 to-indigo-600">

    <h1 class="mb-10 text-5xl font-extrabold text-white">BrainSync Assistant</h1>

    <!-- Register Form -->
    <form action="/register" method="POST" class="w-full max-w-lg p-8 space-y-6 bg-white rounded-lg shadow-xl">
        <div>
            <label for="username" class="text-xl font-semibold text-gray-800">Username</label>
            <input type="text" name="username" id="username" class="w-full p-4 mt-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-teal-500" placeholder="Enter your username" required>
        </div>
        <div>
            <label for="email" class="text-xl font-semibold text-gray-800">Email</label>
            <input type="email" name="email" id="email" class="w-full p-4 mt-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-teal-500" placeholder="youremail@example.com" required>
        </div>
        <div>
            <label for="password" class="text-xl font-semibold text-gray-800">Password</label>
            <input type="password" name="password" id="password" class="w-full p-4 mt-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-teal-500" placeholder="Enter your password" required>
        </div>

        <!-- Phone and Location Inputs in One Row -->
        <div class="grid grid-cols-2 gap-6">
            <div>
                <label for="phone" class="text-xl font-semibold text-gray-800">Phone</label>
                <input type="text" name="phone" id="phone" class="w-full p-4 mt-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-teal-500" placeholder="Your phone number">
            </div>
            <div>
                <label for="location" class="text-xl font-semibold text-gray-800">Location</label>
                <input type="text" name="location" id="location" class="w-full p-4 mt-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-teal-500" placeholder="Your location">
            </div>
        </div>

        <div>
            <label for="gender" class="text-xl font-semibold text-gray-800">Gender</label>
            <select name="gender" id="gender" class="w-full p-4 mt-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-teal-500">
                <option value="">Select</option>
                <option value="Male">Male</option>
                <option value="Female">Female</option>
                <option value="Other">Other</option>
            </select>
        </div>
        <div>
            <label for="role" class="text-xl font-semibold text-gray-800">Role</label>
            <select name="role" id="role" class="w-full p-4 mt-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-teal-500">
                <option value="user" selected>User</option>
                <option value="admin">Admin</option>
            </select>
        </div>

        <button type="submit" class="w-full py-3 text-white rounded-md bg-gradient-to-r from-teal-500 to-blue-600 hover:from-teal-600 hover:to-blue-700 focus:outline-none focus:ring-2 focus:ring-teal-500">Register</button>
    </form>

    <!-- Login Link with Styling -->
    <p class="mt-6 text-sm text-center text-gray-600">
        <span class="text-gray-200">Already have an account?</span> 
        <a href="/" class="text-white font-semibold hover:underline">Login here</a>
    </p>

</body>

</html>

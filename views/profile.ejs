<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Profile</title>
    <!-- Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="flex flex-col min-h-screen font-sans bg-gradient-to-r from-teal-400 via-blue-500 to-indigo-600">

    <!-- Navbar -->
    <nav class="fixed top-0 left-0 right-0 z-10 w-full bg-white shadow-md">
        <div class="container flex items-center justify-between px-6 py-4 mx-auto">
            <h1 class="text-2xl font-bold text-teal-600">BrainSync</h1>
            <div class="space-x-4">
                <a href="/index" class="text-gray-800 hover:text-teal-600">Home</a>
                <a href="/profile" class="text-blue-800 hover:text-teal-600">Profile</a>
                <a href="/edit-profile" class="hover:text-blue-200">Settings</a>
                <% if (user.role === 'admin') { %>
                    <a href="/admin/users" class="text-gray-800 hover:text-teal-600">User List</a>
                <% } %>
                <a href="/logout" class="text-gray-800 hover:text-teal-600">Logout</a>
            </div>
        </div>
    </nav>

    <!-- Profile container -->
    <div class="flex flex-col items-center justify-center w-full h-full mt-24">
        <div class="w-full max-w-3xl p-8 bg-white border-2 border-gray-200 shadow-2xl rounded-2xl">

            <!-- Profile Heading -->
            <h1 class="mb-6 text-4xl font-bold text-center text-transparent bg-gradient-to-r from-pink-500 to-yellow-500 bg-clip-text">
                Your Profile
            </h1>

            <!-- Profile details -->
            <div class="space-y-4">
                <div class="text-xl text-gray-700">
                    <strong>Username:</strong> <span class="font-medium"><%= user.username %></span>
                </div>
                <div class="text-xl text-gray-700">
                    <strong>Email:</strong> <span class="font-medium"><%= user.email %></span>
                </div>
                <div class="text-xl text-gray-700">
                    <strong>Phone:</strong> <span class="font-medium"><%= user.phone %></span>
                </div>
                <div class="text-xl text-gray-700">
                    <strong>Location:</strong> <span class="font-medium"><%= user.location %></span>
                </div>
                <div class="text-xl text-gray-700">
                    <strong>Gender:</strong> <span class="font-medium"><%= user.gender %></span>
                </div>

                <!-- Role Specific Content -->
                <% if (user.role === 'admin') { %>
                    <div class="text-xl text-gray-700">
                        <strong>Admin Panel Access:</strong> <span class="font-medium">Yes</span>
                    </div>
                <% } else { %>
                    <div class="text-xl text-gray-700">
                        <strong>Role:</strong> <span class="font-medium"><%= user.role %></span>
                    </div>
                <% } %>
            </div>

            

        </div>
    </div>

</body>
</html>

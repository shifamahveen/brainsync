<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BrainSync App</title>
    <script src="https://cdn.tailwindcss.com"></script> <!-- Tailwind CDN -->
</head>
<body class="flex flex-col items-center justify-center min-h-screen py-8 font-sans bg-gradient-to-r from-teal-400 via-blue-500 to-indigo-600">

    <!-- Navbar -->
    <nav class="fixed top-0 left-0 right-0 z-10 w-full bg-white shadow-md">
        <div class="container flex items-center justify-between px-6 py-4 mx-auto">
            <h1 class="text-2xl font-bold text-teal-600">BrainSync</h1>
            <div class="space-x-4">
                <a href="/index" class="text-blue-800 hover:text-teal-600">Home</a>
                <a href="/profile" class="text-gray-800 hover:text-teal-600">Profile</a>
                <a href="/edit-profile" class="hover:text-blue-200">Settings</a>
                <% if (user && user.role === 'admin') { %>
                    <a href="/admin/users" class="text-gray-800 hover:text-teal-600">User List</a>
                <% } %>
                <a href="/login" class="text-gray-800 hover:text-teal-600">Logout</a>
            </div>
        </div>
    </nav>
    

    
    <!-- Page Content -->
    <div class="flex flex-col items-center justify-center w-full mt-16">
        <h1 class="mb-10 text-5xl font-extrabold text-white">BrainSync Assistant</h1>

        <!-- Form for input -->
        <form action="/index" method="POST" class="w-full max-w-lg p-8 space-y-6 bg-white rounded-lg shadow-xl">
            <div>
                <label for="userInput" class="text-xl font-semibold text-gray-800">Your Question:</label>
                <input type="text" id="userInput" name="userInput" required class="w-full p-4 mt-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-teal-500" placeholder="Enter your question...">
            </div>
            <button type="submit" class="w-full py-3 text-white rounded-md bg-gradient-to-r from-teal-500 to-blue-600 hover:from-teal-600 hover:to-blue-700 focus:outline-none focus:ring-2 focus:ring-teal-500">Submit</button>
        </form>

        <!-- Response Section -->
        <div class="w-full max-w-lg mt-8">
            <h2 class="mb-4 text-2xl font-semibold text-white">Response:</h2>
            <div class="p-6 text-gray-800 bg-white rounded-lg shadow-xl">
                <p><%= response %></p>
            </div>  
        </div>
    </div>

</body>
</html>

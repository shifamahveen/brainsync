<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Profile</title>
    <!-- Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="flex flex-col min-h-screen font-sans bg-gradient-to-r from-blue-100 via-green-100 to-pink-100">

    <!-- Navbar -->
    <nav class="fixed top-0 left-0 z-10 w-full p-4 bg-blue-600">
        <div class="flex items-center justify-between max-w-6xl mx-auto text-white">
            <a href="/" class="text-2xl font-semibold">StoryGen</a>
            <div class="space-x-6">
                <a href="/" class="hover:text-blue-200">Home</a>
                <a href="/login" class="hover:text-blue-200">Logout</a>
            </div>
        </div>
    </nav>

    <!-- Profile container -->
    <div class="flex flex-col items-center justify-center w-full h-full mt-24">
        <div class="w-full max-w-3xl p-8 bg-white border-2 border-gray-200 shadow-2xl rounded-2xl">

            <!-- Profile Heading -->
            <h1 class="mb-6 text-4xl font-bold text-center text-transparent bg-gradient-to-r from-pink-500 to-yellow-500 bg-clip-text">
                Your Profile
            </h1>

            <!-- Profile details -->
            <div class="space-y-4">
                <div class="text-xl text-gray-700">
                    <strong>Username:</strong> <span class="font-medium"><%= user.username %></span>
                </div>
                <div class="text-xl text-gray-700">
                    <strong>Email:</strong> <span class="font-medium"><%= user.email %></span>
                </div>
                <div class="text-xl text-gray-700">
                    <strong>Phone:</strong> <span class="font-medium"><%= user.phone %></span>
                </div>
                <div class="text-xl text-gray-700">
                    <strong>Location:</strong> <span class="font-medium"><%= user.location %></span>
                </div>
                <div class="text-xl text-gray-700">
                    <strong>Gender:</strong> <span class="font-medium"><%= user.gender %></span>
                </div>

                <!-- Role Specific Content -->
                <% if (user.role === 'admin') { %>
                    <div class="text-xl text-gray-700">
                        <strong>Admin Panel Access:</strong> <span class="font-medium">Yes</span>
                    </div>
                <% } else { %>
                    <div class="text-xl text-gray-700">
                        <strong>Role:</strong> <span class="font-medium"><%= user.role %></span>
                    </div>
                <% } %>
            </div>

            

        </div>
    </div>

</body>
</html>
